{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); cmd=$(printf '%s' \"$input\" | jq -r '.tool_input.command // empty'); if printf '%s' \"$cmd\" | grep -Eq '(^|[[:space:]&|;(])git[[:space:]]+commit([[:space:]]|$)'; then printf '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"REMINDER: If this commit includes major structural changes (new route groups, new providers/contexts, library swaps, animation-system rewrites, data-model changes, or anything that would make CLAUDE.md misleading), update CLAUDE.md in the same commit. Skip if the change is small/localized.\"}}'; fi"
          }
        ]
      }
    ]
  }
}
